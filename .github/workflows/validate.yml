name: validate

on:
  push:
    branches: [main]
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # Pure-stdlib check: manifest conventions, Cursor twin sync, README table.
      - run: python3 scripts/validate.py
