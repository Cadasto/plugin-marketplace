name: validate

on:
  push:
    branches: [main]
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # Pure-stdlib check: conventions, twin sync, README names, changelog heading.
      - run: python3 scripts/validate.py
